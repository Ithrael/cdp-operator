---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: canarydeployplans.k8s.shanbay.io.shanbay.com
spec:
  group: k8s.shanbay.io.shanbay.com
  names:
    kind: CanaryDeployPlan
    listKind: CanaryDeployPlanList
    plural: canarydeployplans
    singular: canarydeployplan
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CanaryDeployPlan is the Schema for the canarydeployplans API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CanaryDeployPlanSpec defines the desired state of CanaryDeployPlan
            properties:
              destination:
                type: string
              policies:
                properties:
                  combine:
                    properties:
                      must:
                        items:
                          properties:
                            op:
                              type: string
                            policy:
                              type: string
                            value:
                              type: string
                          required:
                          - op
                          - policy
                          - value
                          type: object
                        type: array
                    required:
                    - must
                    type: object
                required:
                - combine
                type: object
              service:
                description: Foo is an example field of CanaryDeployPlan. Edit canarydeployplan_types.go
                  to remove/update
                type: string
            required:
            - policies
            - service
            type: object
          status:
            description: CanaryDeployPlanStatus defines the observed state of CanaryDeployPlan
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
